1) Задание в папке 1
2) Задание в папке 2
3) Написать класс CompanySearch с методом search, возвращающим data provider.

В нём сделать фильтрацию по:

- id компании
- slug компании
- части названия компании
- id персоны, работающей в компании
- части имени персоны, работающей в компании

Сортируем по sortOrder, количеству персонала

Важно: используем релейшны, не используем метод leftJoin, т.е. джойны таблиц только через релейшны.

////////////////////////////////////////////

Модель CompanyPerson я бы унаследовал от ActiveQuery и запросы к данным формировал при запросе например так:

$models = Company::find()
    ->withPerson()
    ->withComments()
    ->byPersonId()
    ->all()

Имена методов и их кол-во условное, так как не совсем понятно что конкретно надо будет показывать клиенту.
